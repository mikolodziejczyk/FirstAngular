import { AnotherValidatorDirective } from './another-validator.directive';

describe('AnotherValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AnotherValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
